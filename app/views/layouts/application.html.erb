<!DOCTYPE html>
<html>
  <head>
    <title>FligFlog</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div>
      <ul>
        <li><%= link_to "Sign up", new_pilot_path if !current_pilot %>"/></li>
        <li><%= link_to "Sign in", signin_path if !current_pilot %></li>
        <li><%= link_to "Log Out", logout_path, method: :post if current_pilot %></li>
      </ul>
    </div>
    <div class="container"  role="main">
      <%= content_tag(:div, flash[:notice], :id => "flash_notice") if flash[:notice] %>
      <%= yield %>
    </div>
  </body>
</html>
